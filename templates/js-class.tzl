@if(export) export @endif @if(default) default @endif class ${name}{

    @foreach(prop)
    ${prop.name};
    @endforeach
    
    @if(constructor)
    constructor( @foreach(prop) ${prop.name}, @endforeach ){

    }
    @endif

    
}